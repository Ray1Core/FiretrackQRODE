<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
             x:Class="Firetrack.Views.ScannerPage"
             Title="Scan QR Code"
             Shell.TabBarIsVisible="False"
             BackgroundColor="{StaticResource Background}">

    <Grid>

        <zxing:CameraBarcodeReaderView x:Name="cameraBarcodeReaderView"
                                       BarcodesDetected="OnBarcodesDetected"
                                       IsTorchOn="False"
                                       IsVisible="True"
                                       CameraLocation="Rear" />

        <!-- Placeholder overlay -->
        <VerticalStackLayout IsVisible="{Binding IsPlaceholderVisible}"
                             VerticalOptions="Center"
                             HorizontalOptions="Center"
                             Spacing="10"
                             BackgroundColor="#CC000000">
            <ActivityIndicator IsRunning="True" Color="{StaticResource PrimaryColor}" />
            <Label Text="Initializing camera..."
                   TextColor="White"
                   FontSize="16"
                   HorizontalOptions="Center" />
        </VerticalStackLayout>

        <!-- Overlay UI -->
        <VerticalStackLayout VerticalOptions="End"
                             HorizontalOptions="Fill"
                             Padding="20"
                             Spacing="10"
                             BackgroundColor="#80000000"
                             IsVisible="{Binding IsScanning}">
            <Label Text="Point camera at QR code"
                   TextColor="White"
                   FontSize="18"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />
            <Label Text="{Binding ScanResult}"
                   TextColor="{StaticResource PrimaryColor}"
                   FontSize="14"
                   HorizontalOptions="Center" />

            <!-- ✅ NEW: Manual QR entry fallback for MediaTek devices where
                 ZXing ImageAnalysis fails silently (Realme C100 4G etc.) -->
            <Button Text="⌨️ Type QR Code Manually"
                    Clicked="OnTypeQrClicked"
                    BackgroundColor="#CC4445B1"
                    TextColor="White"
                    BorderColor="White"
                    BorderWidth="1"
                    CornerRadius="8"
                    HeightRequest="44"
                    HorizontalOptions="Center"
                    WidthRequest="240" />

            <Button Text="Cancel"
                    Command="{Binding CancelCommand}"
                    BackgroundColor="Transparent"
                    TextColor="White"
                    BorderColor="White"
                    BorderWidth="1"
                    CornerRadius="8"
                    HeightRequest="44"
                    HorizontalOptions="Center"
                    WidthRequest="120" />
        </VerticalStackLayout>

        <ActivityIndicator IsVisible="{Binding IsBusy}"
                           IsRunning="{Binding IsBusy}"
                           Color="{StaticResource PrimaryColor}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
    </Grid>
</ContentPage>